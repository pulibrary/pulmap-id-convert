# This repo is archived and no longer maintained

### Pulmap ID Translate

#### Build User Interface

```
	$ cd public
	$ npm install
	$ bower install
	$ grunt
```

#### Setup App
```
	$ bundle install
	$ rake db:migrate:up
	$ rake db:seed
	$ thin start
```
